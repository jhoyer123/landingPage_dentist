---
import type { AstroComponentFactory } from "astro/runtime/server/index.js";
import Address from "../../../public/icons/contact/Address.astro";
import Mail from "../../../public/icons/contact/Mail.astro";
import Phone from "../../../public/icons/contact/Phone.astro";
import Whatsapp from "../../../public/icons/contact/Whatsapp.astro";
import Mapa from "./Mapa.astro";
import Form from "./Form.astro";

interface ContactDetail {
  title: string;
  value: string;
  icon: AstroComponentFactory;
}

const contactDetails: ContactDetail[] = [
  {
    title: "Llámanos",
    value: "+591 78788989",
    icon: Phone,
  },
  {
    title: "Escríbenos",
    value: "info@dentalcare.com",
    icon: Mail,
  },
  {
    title: "WhatsApp",
    value: "78594512",
    icon: Whatsapp,
  },
  {
    title: "Consultorio",
    value: "Av. San Martín #234",
    icon: Address,
  },
];
---

<section
  id="contact"
  class="py-20 px-4 relative overflow-hidden bg-linear-to-b from-transparent via-blue-200/50 to-transparent"
>
  <div
    class="max-w-6xl mx-auto text-start mb-16 relative z-10 flex flex-col gap-5"
  >
    <span
      class="text-foreground-muted font-bold tracking-widest text-sm uppercase block font-heading"
    >
      Contacto
    </span>
    <h2
      id="contact-title"
      class="font-heading text-2xl md:text-3xl lg:text-4xl font-bold mt-2 leading-tight max-w-xl text-foreground"
    >
      Ponte en <span class="text-accent">contacto con nosotros</span>
    </h2>
  </div>

  <div class="max-w-6xl mx-auto relative z-10">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 items-start">
      <Form />

      <div class="lg:col-span-5 flex flex-col gap-10 justify-between h-full">
        <div>
          <p class="text-foreground-muted leading-relaxed font-body text-md">
            En nuestra clínica dental, tu sonrisa es nuestra prioridad. Estamos
            disponibles para atender tus dudas y agendar tu próxima visita para
            un cuidado integral.
          </p>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-x-4 gap-y-8">
          {
            contactDetails.map((item) => (
              <div class="flex flex-col items-center justify-center gap-3 group bg-gray-50 border border-gray-300 p-3 rounded-2xl">
                <div class="p-3 bg-white rounded-xl shadow-sm border border-slate-100 text-accent group-hover:bg-accent group-hover:text-white transition-colors duration-300 font-body">
                  <item.icon class="w-6 h-6" />
                </div>
                <div class="w-full flex flex-col items-center justify-center">
                  <h4 class="font-heading font-bold text-foreground text-sm">
                    {item.title}
                  </h4>
                  <p class="font-body text-foreground-muted text-sm mt-1">
                    {item.value}
                  </p>
                </div>
              </div>
            ))
          }
        </div>

        <Mapa />
      </div>
    </div>
  </div>
</section>
