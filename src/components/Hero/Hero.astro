---
import { Image } from "astro:assets";
import heroImage from "../../images/heroImage.png";
import BrandSlide from "./BrandSlide.astro";

import heroBackgroundMob from "../../images/heroBackgroundMob.jpg";
import heroBackgroundDesk from "../../images/heroBackgrundDesk.jpg";
---

<section
  class="relative min-h-screen py-20 sm:pt-24 flex items-center overflow-hidden"
  aria-labelledby="hero-title"
>
  <div
    aria-hidden="true"
    class="absolute inset-0 bg-cover bg-center bg-no-repeat
    mask-[linear-gradient(to_bottom,black_50%,transparent_100%)]
    opacity-30"
  >
    <!-- Imagen de fondo para móvil -->
    <Image
      src={heroBackgroundMob}
      alt=""
      width={768}
      height={1024}
      format="webp"
      quality={75}
      loading="eager"
      class="absolute inset-0 w-full h-full object-cover md:hidden"
    />

    <!-- Imagen de fondo para desktop -->
    <Image
      src={heroBackgroundDesk}
      alt=""
      width={1920}
      height={1080}
      format="webp"
      quality={75}
      loading="eager"
      class="hidden md:block absolute inset-0 w-full h-full object-cover"
    />
  </div>

  <div
    class="relative z-10 mx-auto w-full max-w-6xl flex flex-col items-center justify-center px-4"
  >
    <div
      class="w-full flex flex-col items-center justify-center
      sm:flex-row-reverse sm:items-center md:gap-20 lg:gap-30"
    >
      <div
        class="drop-shadow-[0_20px_60px_rgba(59,130,246,0.90)] md:drop-shadow-[0_20px_60px_rgba(6,182,212,0.3)]"
      >
        <Image
          src={heroImage}
          alt="Dentista profesional brindando atención de calidad"
          widths={[160, 240, 280, 400, 640]}
          height={900}
          format="webp"
          quality={85}
          sizes="(max-width: 640px) 160px, (max-width: 1024px) 240px, 280px"
          loading="eager"
          fetchpriority="high"
          class="w-30 sm:w-46 md:w-45 lg:w-60 mask-[linear-gradient(to_bottom,black_85%,transparent_100%)]"
        />
      </div>

      <div
        class="flex flex-col sm:gap-10 md:gap-15 items-center sm:items-start text-center sm:text-start"
      >
        <!-- Título principal -->
        <h1
          id="hero-title"
          class="font-heading font-extrabold text-2xl sm:text-4xl md:text-5xl lg:text-6xl max-w-100"
        >
          Tu <span class="text-accent">Sonrisa</span> en manos de
          <span class="text-accent">Profesionales</span>
        </h1>

        <!-- Descripción -->
        <p
          class="font-body text-sm sm:text-base lg:text-lg text-gray-700 md:text-foreground-muted max-w-md leading-relaxed bg-white/10 md:bg-white-30 backdrop-blur-sm px-4 py-3 rounded-lg border border-white/20"
        >
          Atención odontológica integral para niños y adultos, con tecnología
          moderna y un trato humano.
        </p>
        <div class="hidden sm:flex">
          <div class="flex flex-wrap gap-10">
            <a
              href="#contact"
              class="group relative px-8 py-3.5 bg-accent text-white rounded-full
              shadow-lg transition-all duration-300
              hover:scale-105 active:scale-95"
              aria-label="Contactar con nuestro equipo odontológico"
            >
              <span
                class="font-accent relative z-10 font-medium text-sm md:text-md lg:text-lg"
                >Contáctanos</span
              >
            </a>

            <a
              href="#services"
              class="group relative px-8 py-3.5 bg-white text-gray-700 font-medium text-sm rounded-full
              border-2 border-gray-200 shadow-md transition-all duration-300
              hover:border-accent hover:text-accent hover:scale-105 active:scale-95"
              aria-label="Ver nuestros servicios odontológicos"
            >
              <span
                class="font-accent relative z-10 font-medium text-sm md:text-md lg:text-lg"
                >Servicios</span
              >
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
  <BrandSlide />
</section>
